
const initalState={message:`this message is from reducer script.`};

function reducer(state = initalState)
{
    //we will set up a switch to go between states
    return state;
}

export default reducer;